# Usage

Setup:

```
# Using Python 3
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Start the server:
```
python server.py
```

Make a request:
```
curl http://localhost:8080/secs/3,5
```

